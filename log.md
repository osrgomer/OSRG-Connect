# Activity Log

## 2026-03-17

- Created this log file to track actions on demand.
- Added a pending-user reject handler in `admin.php` so admins can delete unapproved accounts and notify users by email when rejecting.
- Added a pre-game car selection interface to `sp_games/valley_game`, including placeholder cards and selection wiring so the player can choose a ride before starting.
